const layOutHorcruxes=()=>{const available=Math.max(0,Math.min(720,window.innerWidth*.9-210));document.querySelectorAll('.horcrux-step').forEach(step=>{step.style.gridTemplateColumns=window.innerWidth<=650?'minmax(0, 1fr)':`160px minmax(0, ${available}px)`;step.style.justifyContent='start';step.style.maxWidth='100%';const content=step.querySelector('div');if(content){content.style.minWidth='0';content.style.width='100%';content.style.maxWidth=`${available}px`}const paragraph=step.querySelector('p');if(paragraph){paragraph.style.display='block';paragraph.style.width='100%';paragraph.style.maxWidth=`${available}px`;paragraph.style.minWidth='0';paragraph.style.whiteSpace='normal';paragraph.style.overflowWrap='anywhere'}})};layOutHorcruxes();window.addEventListener('resize',layOutHorcruxes);
