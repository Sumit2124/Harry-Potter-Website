// Loader intentionally disabled during the interaction-stability pass.
// Keeping this file as a no-op prevents older page references from creating a
// full-screen overlay that can interfere with scroll or touch input.
window.__magicalPageLoader = true;
